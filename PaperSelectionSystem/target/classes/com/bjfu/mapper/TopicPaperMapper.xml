<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bjfu.mapper.TopicPaperMapper">
	<select id="selectTopicPaper" resultType="com.bjfu.po.Paper">
	 	select paper.*
	 	from topicpaper inner join paper on topicpaper.paper_id=paper.paper_id
	 	where stutopic_id=#{stutopic_id}
	 </select>
</mapper>

